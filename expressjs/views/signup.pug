html
    head
        title Sign Up
    body
        if(message)
            h4 #{message}
        form(name="auth" action="/signup" method="post")
            input(name="id" type="text" required placeholder="user id" )
            input(name="password" type="password" required placeholder="password" )
            button(type="submit") Sign me up!
